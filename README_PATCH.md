# Patch para erro Models/Services

Envie as pastas `Models` e `Services` para a raiz do repositório GitHub, no mesmo nível de `MainWindow.xaml.cs` e `NexXSensi.WinUI3.csproj`.

Arquivos incluídos:

- Models/ActionResult.cs
- Models/OptimizationAction.cs
- Services/ScriptRunner.cs

Depois rode novamente o workflow Actions.
